import React from 'react';
import ReactLoading from 'react-loading';

const Loading = () => <ReactLoading color={'#FF9292'} type={'spinningBubbles'} height={'100px'} width={'100px'} />;

export default Loading;
